{
    "database": "postgres",
    "credentials": {
        "host": "db",
        "port": 5432,
        "user": "${POSTGRES_USER}",
        "password": "${POSTGRES_PASSWORD}",
        "database": "dataflow"
    },
    "template": "default",
    "evidenceDir": ".evidence"
}
